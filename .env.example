# AviationStack API key (free tier)
# Get one at https://aviationstack.com/signup/free
# Server-side only — used by the /api/flights proxy.
# When set, live flight data enriches mock trips.
# When absent, the app uses mock data only (no behavior change).
FLIGHT_API_KEY=
